// use this to build the webpage output from the past and test commands
// will need to be included in those js files

// should be able to create the results in a text file on the server
// then use this script to loop through and output to an HTML table